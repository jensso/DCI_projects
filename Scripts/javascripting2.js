let shop = {
  tshirtPrice : 10,
  jeansPrice : 30,
  jacketPrice : 150,
  tshirtStock : 100,
  jeansStock : 100,
  jacketStock : 100,
  balance : 0,
  sale: function(numShirts,numJeans,numJackets) {
    if ()
  }
};

let cashier = function(id, employee,) {
  this.id = id;
  this.employee = employee;
  this.numOdSales = 0;
  this.cashierBalance = 0;
}

let cashier1 cashier(1) = new cashier(1,`Hans`);
let cashier2 cashier(2) = new cashier(2, `Peter`);
let cashier3 cashier(3) = new cashier(3, `Paul`);

Object.setPrototypeOf(cashier1,cashier);
Object.setPrototypeOf(cashier2,cashier);
Object.setPrototypeOf(cashier3,cashier);
