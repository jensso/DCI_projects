var optionI = `furry animal`;
var optionII = `short term for category`;
var optionIII = `causes bleeding when done to a finger`;

if (optionII || optionIII) {
  console.log(`pronounce it: "Katt"`);
}
  else if (optionI) {
    console.log(`pronounce it: "Kett"`);
    }
    else {
      console.log(`go to London && Sydney && Houston && Glasgow && Boston
                    and tell some folks about correct pronouncation!`);
    }
// I wrote some code about a `thin` + `g||k` I had to `thin` + `g||k` about way too much...
